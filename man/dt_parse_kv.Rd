% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datatable.R
\name{dt_parse_kv}
\alias{dt_parse_kv}
\title{Parse and Expand Key-Value Column in a data.table}
\usage{
dt_parse_kv(
  dt_input,
  col2split,
  split1 = ";",
  split2 = "=",
  delete = TRUE,
  verbose = FALSE,
  byref = TRUE,
  keep_keys = NULL,
  prefix = NULL
)
}
\arguments{
\item{dt_input}{A \code{data.table} containing the column to be parsed.}

\item{col2split}{A character string giving the name of the column in \code{dt_input} that contains key-value strings.}

\item{split1}{Character to split key-value pairs (default is \code{";"}).}

\item{split2}{Character to split key from value (default is \code{"="}).}

\item{delete}{Logical. If \code{TRUE}, the original column (\code{col2split}) will be removed from the output (default \code{TRUE}).}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages (default \code{FALSE}).}

\item{byref}{Logical. If \code{TRUE}, modifies the input \code{data.table} by reference; if \code{FALSE}, works on a copy (default \code{TRUE}).}

\item{keep_keys}{Optional character vector of key names to keep. If \code{NULL}, all parsed keys will be retained.}

\item{prefix}{Optional string to prefix all new column names (default \code{NULL}).}
}
\value{
A \code{data.table} with the same rows as \code{dt_input}, with parsed key-value pairs added as new columns.
If \code{byref = TRUE}, the input is modified by reference and also returned.
}
\description{
Splits a column of key-value pairs (e.g. \code{INFO} from VCF-like data) into multiple columns,
using efficient string operations and data.table reshaping. Supports optional prefixing,
key filtering, and in-place or copy behavior.
}
\examples{
dt <- data.table(ID = 1:2,
                 INFO = c("A=1;B=2;C=3", "B=20;C=30;D=40"))
dt_parsed <- dt_parse_kv(dt, col2split = "INFO", keep_keys = c("B", "C"))

}
