% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{labels_from_index_list}
\alias{labels_from_index_list}
\title{Vectorized paste of labels for each row's index list}
\usage{
labels_from_index_list(
  idx_list,
  rule_labels,
  collapse = "; ",
  deduplicate = TRUE,
  ignore_case = FALSE,
  no_match_value = NA_character_,
  validate_bounds = TRUE
)
}
\arguments{
\item{idx_list}{List where each element is an integer vector of indices
(1-based) into \code{rule_labels}. Length should equal the number of rows/events.}

\item{rule_labels}{Character vector of labels to index into.}

\item{collapse}{Character scalar used to join multiple labels. Default: "; ".}

\item{deduplicate}{Logical; remove duplicates while preserving order. Default: TRUE.}

\item{ignore_case}{Logical; case-insensitive de-duplication if TRUE. Default: FALSE.}

\item{no_match_value}{Character to return when an element of \code{idx_list} is empty.
Default: NA_character_.}

\item{validate_bounds}{Logical; error if any indices are out of bounds. Default: TRUE.}
}
\value{
Character vector of length \code{length(idx_list)}.
}
\description{
Vectorized paste of labels for each row's index list
}
