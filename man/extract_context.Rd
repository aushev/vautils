% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{extract_context}
\alias{extract_context}
\title{Extract a regex keyword with one word of context on each side}
\usage{
extract_context(text, keyword)
}
\arguments{
\item{text}{A character vector. Each element is processed independently.}

\item{keyword}{A character string containing a regular expression that
defines the keyword to match. This may include character classes,
quantifiers, alternation (\code{|}), or any valid regex construct.}
}
\value{
A character vector of the same length as \code{text}, containing either
the extracted context or \code{NA} if no keyword match is found.
}
\description{
This function searches a character vector for a specified \strong{regular-expression
keyword} and returns the keyword match together with at most one \emph{preceding}
and one \emph{following} word.
}
\details{
A “word” is defined as a sequence of letters (\verb{A–Z} / \verb{a–z}).
Any sequence of non-letter characters (spaces, punctuation, slashes,
hyphens, periods, etc.) is treated as a “separator”.

The extraction pattern is:

\preformatted{
  [word][separator](keyword-regex)[separator][word]
}

where each of the surrounding \verb{[word]} blocks is optional, and the
central \code{(keyword-regex)} is inserted exactly as given (it is \strong{not}
escaped or treated literally).
}
\examples{
# literal keyword
extract_context("I bought a big apple at the store", "apple")
# "big apple at"

# keyword as regex ("appl.*at" matches "apple at")
extract_context("I bought a big apple at the store", "appl.*at")
# "big apple at the"

# punctuation and compound words
extract_context("I bought a big apple-like fruit.", "apple.*")
# "big apple-like fruit."

extract_context("I bought a big pear/apple there.", "pear.*apple")
# "big pear/apple there"

# keyword at boundaries
extract_context("apple at the store", "appl.*at")
# "apple at the"

}
