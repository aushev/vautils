% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{split_count}
\alias{split_count}
\title{Count Unique Substring Matches from Delimited Strings}
\usage{
split_count(input, sep = ";")
}
\arguments{
\item{input}{A character vector with delimited entries (e.g., "A; B; C").}

\item{sep}{A character string used as the delimiter between items (default: \code{";"}).}
}
\value{
A \code{data.table} with two columns:
\describe{
\item{value}{The unique trimmed values extracted from the input}
\item{count}{The number of elements in \code{input} that contain the value}
}
}
\description{
This function takes a character vector where each element contains one or more items
separated by a specified delimiter. It returns a table with the count of how many
times each unique item appears as a substring match across all elements.
}
\details{
This function performs substring matching using \code{grepl}.
Matching is case-sensitive.
}
\examples{
input <- c("apple; banana", "banana; cherry", "apple", "banana")
split_count(input)

}
\seealso{
\code{\link{strsplit}}, \code{\link[data.table]{data.table}}
}
