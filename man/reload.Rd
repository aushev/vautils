% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/000loading.R
\name{reload}
\alias{reload}
\title{Detach (if attached) and re-attach a package by name}
\usage{
reload(pkgName)
}
\arguments{
\item{pkgName}{\code{character(1)}. The package name (e.g., \code{"dplyr"}).}
}
\value{
A logical value indicating whether the package
was successfully attached (\code{TRUE}) or not (\code{FALSE}). Messages are printed
for user feedback.
}
\description{
Convenience helper that checks whether a package is currently \strong{attached}
on the search path and, if so, detaches it, then (re)loads it via
\code{\link[=reqS]{reqS()}}. This is handy during interactive development when you want to
pick up changes to a dependency without restarting R.
}
\details{
\itemize{
\item Attachment is detected via \code{\link[=search]{search()}}; if \code{package:PKG} is present, the
function prints a message and calls \code{\link[=unloadNamespace]{unloadNamespace()}} on \code{PKG} before
reloading it with \code{\link[=reqS]{reqS()}}.
\item If the package namespace is loaded but not attached, it may still be
unloaded by \code{unloadNamespace()}. Detach failures can occur if other loaded
packages depend on \code{pkgName}.
}
}
\section{Important}{

This function performs side-effects on the session search path and namespace
table. Avoid using it inside package code or scripts where reproducibility
matters. Prefer declaring dependencies in \code{DESCRIPTION} and using
\code{Imports}/\code{NAMESPACE} rather than attaching/detaching at runtime.
}

\examples{
\dontrun{
  # Reload ggplot2 interactively
  reload("ggplot2")

  # Use within a development session after editing a dependency
  reload("data.table")
}

}
\seealso{
\code{\link[=search]{search()}}, \code{\link[=detach]{detach()}}, \code{\link[=unloadNamespace]{unloadNamespace()}}
}
