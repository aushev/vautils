% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{paste_labels_by_index}
\alias{paste_labels_by_index}
\title{Paste labels selected by indices (order-preserving)}
\usage{
paste_labels_by_index(
  indices,
  labels,
  collapse = "; ",
  deduplicate = TRUE,
  ignore_case = FALSE,
  no_match_value = NA_character_,
  validate_bounds = TRUE
)
}
\arguments{
\item{indices}{Integer vector of 1-based positions into \code{labels}. May be empty.}

\item{labels}{Character vector containing candidate labels.}

\item{collapse}{Character scalar used to join multiple labels. Default: "; ".}

\item{deduplicate}{Logical; if \code{TRUE}, remove duplicates while preserving the order
of first appearance. Default: \code{TRUE}.}

\item{ignore_case}{Logical; if \code{TRUE} and \code{deduplicate = TRUE}, deduplication is
performed case-insensitively (order still preserved). Default: \code{FALSE}.}

\item{no_match_value}{Character scalar to return when \code{indices} is empty
(i.e., no matches). Default: \code{NA_character_}.}

\item{validate_bounds}{Logical; if \code{TRUE}, checks that all \code{indices} are within
\code{1:length(labels)} and errors otherwise. Default: \code{TRUE}.}
}
\value{
A length-1 character vector (possibly \code{NA_character_} if no matches and
\code{no_match_value} is left at default).
}
\description{
Given a vector of indices (1-based) that point into a character vector of labels,
return a single pasted string.
}
\examples{
labels_vec <- c("Apple", "Banana", "apple", "Cherry")
paste_labels_by_index(
  indices         = c(1L, 3L, 2L, 2L),
  labels          = labels_vec,
  collapse        = ", ",
  deduplicate     = TRUE,
  ignore_case     = TRUE,
  no_match_value  = NA_character_,
  validate_bounds = TRUE
)
# "Apple, Banana"

paste_labels_by_index(
  indices         = integer(0),
  labels          = labels_vec,
  collapse        = "; ",
  deduplicate     = TRUE,
  ignore_case     = FALSE,
  no_match_value  = ""
)
# "" (empty string when no matches)
}
