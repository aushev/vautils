% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/000loading.R
\name{reqq}
\alias{reqq}
\title{Install (if needed) and attach one or more packages}
\usage{
reqq(packagename, verbose = F, tryBioconductor = T, reload = T)
}
\arguments{
\item{packagename}{A package identifier provided as a symbol, a character
string, a space-separated string of names, or a character vector of names.
Internally, symbols are normalized to character names. Space-separated
strings are split and processed element-wise.}

\item{verbose}{\code{logical(1)}. If \code{TRUE}, prints progress messages.}

\item{tryBioconductor}{\code{logical(1)}. If \code{TRUE}, try installing from
Bioconductor (via \strong{BiocManager}) when CRAN install fails.}

\item{reload}{\code{logical(1)}. If \code{TRUE} and more than one package was processed,
\code{vautils} is reloaded (\code{unloadNamespace("vautils")} then \code{require(vautils)}).}
}
\value{
\code{logical(1)}. For a single package, returns the success status from
the underlying loader (\code{TRUE} if attached, \code{FALSE} otherwise). For multiple
packages, returns \code{TRUE} after processing (individual failures are reported
during the run).
}
\description{
Convenience wrapper that accepts a package name as a symbol (e.g. \code{dplyr}),
a single character string (e.g. \code{"dplyr"}), a space-separated string
(e.g. \code{"dplyr ggplot2"}), or a character vector (e.g. \code{c("dplyr","ggplot2")}),
and ensures each package is installed and attached. If a package is not
available from CRAN and \code{tryBioconductor = TRUE}, a Bioconductor install is
attempted via \strong{BiocManager}.
}
\details{
When multiple packages are supplied and \code{reload = TRUE}, the function
unloads and re-attaches \strong{vautils} at the end (to refresh its search path
after attachments).
}
\section{Important}{

This helper is intended for \strong{interactive scripts}. Do \strong{not} use it inside
package code; packages should declare dependencies in \code{DESCRIPTION} and use
\code{Imports}/\code{NAMESPACE} (e.g., \code{pkg::fun} or \verb{@importFrom}), not attach packages
at runtime.
}

\examples{
\dontshow{old_opt <- options(warn = 1); on.exit(options(old_opt), add = TRUE)}
# Interactive usage (do not run in package examples to avoid installs):
\dontrun{
  # Symbol, single name
  reqq(dplyr)

  # Character string
  reqq("dplyr")

  # Space-separated string
  reqq("dplyr ggplot2", verbose = TRUE)

  # Character vector; skip vautils reload at the end
  reqq(c("dplyr", "ggplot2"), reload = FALSE)

  # Try Bioconductor if not on CRAN
  reqq("BiocGenerics", tryBioconductor = TRUE)
}

}
\seealso{
\code{\link[BiocManager:install]{BiocManager::install()}}, \code{\link[=library]{library()}}, \code{\link[=require]{require()}}
}
