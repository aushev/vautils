% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{cs}
\alias{cs}
\title{Split a Delimited Character String}
\usage{
cs(inputstr, sep = ",", fixed = T, nonewlines = T, allowempty = F)
}
\arguments{
\item{inputstr}{A character string or vector of strings to be split.}

\item{sep}{Separator character to split by. Defaults to \code{","}. If missing, it attempts
to guess the separator based on the presence of commas, tabs, or spaces.}

\item{fixed}{Logical. If \code{TRUE} (default), the separator is treated as a fixed string.
If \code{FALSE}, it is treated as a regular expression.}

\item{nonewlines}{Logical. If \code{TRUE} (default), newline characters (\verb{\\n}, \verb{\\r}) are first
replaced by the separator before splitting.}

\item{allowempty}{Logical. If \code{FALSE} (default), empty elements (\code{""}) are removed from the result.}
}
\value{
A character vector of split elements.
}
\description{
Wrapper around \code{strsplit()}
Splits one or more input strings by a specified separator into a character vector.
This function is similar to \code{strsplit()} but with added flexibility for newline handling,
automatic separator guessing, and filtering of empty values.
}
\examples{
cs("apple,banana,pear")
cs("a\tb\tc", sep = "\t")
cs("line1\nline2", sep = ";", nonewlines = TRUE)
cs("a,,b", sep = ",", allowempty = TRUE)

}
