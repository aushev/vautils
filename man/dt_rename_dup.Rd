% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datatable.R
\name{dt_rename_dup}
\alias{dt_rename_dup}
\title{Rename duplicated column in a data.table}
\usage{
dt_rename_dup(dtIn, searchFor, renameTo)
}
\arguments{
\item{dtIn}{A \code{data.table} object in which column names will be modified.}

\item{searchFor}{A \code{character} scalar indicating the name of the column to search for. All columns with this exact name will be targeted.}

\item{renameTo}{A \code{character} vector of replacement names. Must have the same length as the number of matching columns.}
}
\value{
The modified \code{data.table}, invisibly.
}
\description{
Renames all instances of a specified column name in a data.table object. Useful when duplicate column names exist,
and a one-to-one replacement is needed for each instance.
}
\details{
This function is intended for cases where the same column name appears multiple times in a \code{data.table}. It finds all
columns matching \code{searchFor} and renames them according to the elements of \code{renameTo}. It prints the renaming
operations to the console for clarity.

An error is thrown if:
\itemize{
\item \code{searchFor} is not found in any column names;
\item The number of matches does not match the length of \code{renameTo}.
}
}
\examples{
dt <- data.table::data.table(a = 1:3, b = 4:6, a = 7:9)
dt <- dt_rename_dup(dt, "a", c("a1", "a2"))
names(dt)

}
